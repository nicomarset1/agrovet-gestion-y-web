import { SpecialPageShell } from "@/components/special-page-shell";

export const metadata = { title: "Envios" };

export default function EnviosPage() {
  return (
    <SpecialPageShell
      description="Pronto vamos a detallar zonas, tiempos, condiciones y costos de envio."
      title="Envios"
    />
  );
}
